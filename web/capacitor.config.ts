import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.homehealth.clinical',
  appName: 'Home Health Clinical',
  webDir: 'dist'
};

export default config;
