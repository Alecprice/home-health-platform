# Synthetic demo seed

The UI currently embeds synthetic demo data so the product can be demonstrated without a database. Database seed scripts will be added when API-backed patient/visit flows are implemented.

Never put real patient names, MRNs, recordings, or documents in this repository.
